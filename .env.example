# Puerto de la API central
PORT=2090

# URL interna del motor ytdlp-server (solo para llamadas server-to-server)
SCRAPER_URL=http://localhost:4000

# URL publica de esta API (la que el navegador del usuario usa para descargar)
PUBLIC_URL=https://api.devmatrixs.lat

# ── IA Providers ──────────────────────────────────────────
# SambaNova (DeepSeek-V3.1, DeepSeek-R1, etc.)
SAMBANOVA_API_KEY=tu_key_aqui

# Groq (Llama3, Mixtral, Gemma2) — fallback gratuito
GROQ_API_KEY=tu_key_aqui
